{
  "name": "printstation",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "apps/*",
    "apps/print-station",
    "packages/*"
  ],
  "scripts": {
    "dev": "concurrently \"npm run dev -w apps/cloud-api\" \"npm run dev -w apps/customer\" \"npm run dev -w apps/print-station\""
  },
  "engines": {
    "node": ">=22.0.0"
  },
  "devDependencies": {
    "concurrently": "^10.0.5"
  }
}
