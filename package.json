{
  "name": "printstation",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "apps/*",
    "apps/print-station/client",
    "packages/*"
  ],
  "scripts": {
    "dev": "npm run dev --workspaces --if-present"
  },
  "engines": {
    "node": ">=22.0.0"
  }
}
